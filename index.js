const numbers = [1, 2, 3, 4];

//push, unshift, splice for adding
//pop, shift, splice for removing

//End
console.log(numbers.pop()); //4

//Beginning
console.log(numbers.shift()); //1

//Middle
const numbers2 = [1, 2, 3, 4];
console.log(numbers2.splice(2, 2)); // array of 3, 4 because they were removed
